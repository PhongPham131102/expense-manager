import React from "react";
import { View } from "react-native";

// This screen is never shown because we use tabBarButton to override the default behavior
export default function AddScreen() {
  return <View />;
}
